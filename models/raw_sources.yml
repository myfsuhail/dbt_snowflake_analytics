version: 2

sources:
  - name: raw_schema
    database: '{{target.name}}'
    schema: raw
    tables:
      - name: customer
      - name: region
      - name: nation

  - name: consumption
    database: '{{target.name}}'
    schema: consumption
    tables:
      - name: apac_customers

  - name: raw_conn
    database: '{{target.name}}'
    schema: raw2
    tables:
      - name: customer
      - name: region
      - name: nation
